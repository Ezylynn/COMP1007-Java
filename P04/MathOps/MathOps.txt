START MathsOps
  START MAIN
  WHILE continueLoop == TRUE THEN
    TRY 
    PRINT("Welcome to PDI, week 3!")
    PRINT("What would you like to do?")
    PRINT(">1. Sum of 2 integers")
    PRINT(">2. Product of 2 integers")
    PRINT(">3. Determine if 2 Integers are Divisible")
    PRINT(">0. Exit")

    userInput = INPUT("Enter your choice: ")
      CASE userInput OF
        1 
          num1 = INPUT "Enter Num 1: "
          num2 = INPUT "Enter Num 2: "
          numsResult = num1 + num2
          PRINT "Result: " + numsResult
        2 :
          num1 = INPUT "Enter Num 1: "
          num2 = INPUT "Enter Num 2: "
          numsResult = num1 * num2
          PRINT "Result: " + numsResult
        3 :
          num1 = INPUT "Enter Num 1: "
          num2 = INPUT "Enter Num 2: "
          IF (num1%num2==0 || num2%num1==0)
            PRINT "The two integers are divisible by each other"
          ELSE 
            PRINT "The two integers are not divisible by each other"
        0 : 
          PRINT ("End Program")
          continueLoop = FALSE
        OTHERWISE OUTPUT "Invalid input, try again"
      ENDCASE
    CATCH (Exception) 
        OUTPUT "Invalid input, error occured: " + Exception

  ENDWHILE
  END MAIN
END
