START ASCII_Converter
  START Main
    PRINT("What do you want to convert your character into?")
    PRINT("\T (U)ppercase")
    PRINT("\T (L)owercase")
    userInput = INPUT()

    IF (userInput == 'U' || userInput == 'u') THEN
      upChar = INPUT ("Please enter an uppercase char: ")
      lowCharNum = (INT) upChar + 32
      lowChar = (CHAR) lowCharNum 
      PRINT(lowChar)

    ELSE IF (userInput == 'L' || userInput == 'l')THEN
      lowChar= INPUT ('Please enter an lowercase char: ')
      upCharNum = (INT) lowChar - 32
      upChar = (CHAR) upCharNum  
      PRINT(upChar)
      ELSE THEN
        PRINT("Input Invalid. Terminate Program")
    END IF 
    
    OUTPUT("ASCII value of " + UppercaseChar +" is " +UpCharNum)
    OUTPUT("ASCII value of" + LowChar + " is " + "LowCharNum")

  END Main
END 
